# MLflow Project-Specific Configuration
# Copy this file to .env and fill in your values
# The .env file is automatically loaded and will NOT conflict with your global AWS credentials

# MLflow Tracking Server URI (default: http://localhost:5000)
MLFLOW_TRACKING_URI=http://localhost:5000

# === S3 Storage Configuration (Optional) ===
# Uncomment and configure these to use S3 for artifact storage

# S3 Artifact Root - where MLflow will store artifacts
# Example: s3://my-bucket/mlflow-artifacts
# MLFLOW_ARTIFACT_ROOT=s3://YOUR-BUCKET-NAME/mlflow-artifacts

# === OPTION 1: Project-Specific AWS Credentials (Recommended) ===
# These MLFLOW_AWS_* credentials take precedence and won't affect other projects
# MLFLOW_AWS_ACCESS_KEY_ID=your-mlflow-project-access-key-id
# MLFLOW_AWS_SECRET_ACCESS_KEY=your-mlflow-project-secret-key
# MLFLOW_AWS_DEFAULT_REGION=us-east-1

# === OPTION 2: Global AWS Credentials (Fallback) ===
# If MLFLOW_AWS_* are not set, these standard AWS_* variables will be used
# Note: These may conflict with other AWS projects
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_DEFAULT_REGION=us-east-1

# S3 Endpoint URL - only needed for custom S3 endpoints (MinIO, LocalStack, etc.)
# MLFLOW_S3_ENDPOINT_URL=http://localhost:9000

# === S3 Performance Tuning (Optional) ===
# AWS_S3_MAX_CONCURRENT_REQUESTS=50
# AWS_S3_MULTIPART_THRESHOLD=8388608
# AWS_S3_MULTIPART_CHUNKSIZE=8388608
